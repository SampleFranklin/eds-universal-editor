version: 1
sitemaps:
  default:
    source: /query-index.json
    destination: /sitemap.xml
    lastmod: DD-MM-YYYY
  en:
    source: /en/query-index.json
    destination: /sitemap-en.xml
    lastmod: DD-YYYY-MM